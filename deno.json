{
  "compilerOptions": {
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true
  },
  "tasks": {
    "clean": "rimraf dist",
    "dev": "deno run --watch --check --allow-net --allow-read --allow-env ./src/dev.ts",
    "build": "npm run clean && tsc",
    "lint": "deno lint"
  }
}